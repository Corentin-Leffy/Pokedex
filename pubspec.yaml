name: pokedex
description: Pokédex fait avec Flutter

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  data_connection_checker: ^0.3.4
  dartz: ^0.8.9
  equatable: ^1.0.3
  http: ^0.12.0+4
  path: ^1.6.4
  sqflite: ^1.2.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.0

flutter:
  uses-material-design: true